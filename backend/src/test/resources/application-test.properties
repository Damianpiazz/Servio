spring.config.activate.on-profile=test
spring.thymeleaf.prefix=classpath:/templates/

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.jdbc.time_zone=UTC

# JWT
application.security.jwt.secret-key=404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
application.security.jwt.expiration=86400000
application.security.jwt.refresh-token.expiration=604800000

# Frontend
app.frontend-url=http://localhost:3001

# MAIL ? sobreescrito por Testcontainers via @DynamicPropertySource
mail.host=localhost
mail.port=1025
mail.default-sender=no-reply@servio.com
spring.mail.host=localhost
spring.mail.port=1025
spring.mail.properties.mail.smtp.auth=false
spring.mail.properties.mail.smtp.starttls.enable=false

# OBJECT STORAGE ? sobreescrito por Testcontainers via @DynamicPropertySource
object-storage.endpoint=http://localhost:9000
object-storage.access-key=servio
object-storage.secret-key=servio123
object-storage.bucket=servio-assets

# Image upload
image.upload.max-size-bytes=2097152
image.upload.allowed-types=image/jpeg,image/png,image/webp

# Identity
identity.user.logo.folder=logos